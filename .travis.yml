language: node_js

# test on different versions of node.js
node_js:
    - 10.15.2
    - 11.10.1

# configure notifications
notifications:
    email:
        recipients:
          - maurizio.cescon@gmail.com
        on_success: change # default: change
        on_failure: always # default: always
